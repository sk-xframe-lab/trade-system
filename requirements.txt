# ─── Web フレームワーク ───────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ─── DB（PostgreSQL + 非同期 ORM）────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0          # async PostgreSQLドライバ（本番・テスト用）
psycopg2-binary>=2.9.9   # Alembicマイグレーション用（同期ドライバ）
alembic>=1.13.0

# ─── Redis（冪等性キー・分散ロック）────────────────────────────────────────
redis[hiredis]>=5.0.0

# ─── バリデーション・設定 ─────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0

# ─── HTTP クライアント（Broker API 呼び出し）────────────────────────────────
httpx>=0.27.0

# ─── テスト ────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
httpx>=0.27.0             # TestClient 用（上記と共用）
aiosqlite>=0.20.0         # テスト用インメモリDB

# ─── 暗号化 ────────────────────────────────────────────────────────────────
cryptography>=42.0.0     # AES-256-GCM（TOTP シークレット暗号化）
pyotp>=2.9.0             # TOTP 生成・検証（Google Authenticator 互換）

# ─── ユーティリティ ────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tzdata>=2024.1        # zoneinfo (stdlib) がタイムゾーンデータを見つけられない環境向け
